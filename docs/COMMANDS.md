# omni Command Reference

<!-- This file is auto-generated by tools/cmdref/cmdref.go -->
<!-- Run: go run tools/cmdref/cmdref.go   (or: task docs:commands) -->

## Core Commands

### basename - Strip directory and suffix from file names
```bash
omni basename NAME [SUFFIX] [flags]
  -s, --suffix string       remove a trailing SUFFIX
```

### cat - Concatenate files and print on the standard output
```bash
omni cat [file...] [flags]
  -e, --e                   equivalent to -vE
      --json                output as JSON array of lines
  -n, --number              number all output lines
  -b, --number-nonblank     number nonempty output lines, overrides -n
  -A, --show-all            equivalent to -vET
  -E, --show-ends           display $ at end of each line
  -v, --show-nonprinting    use ^ and M- notation, except for LFD and TAB
  -T, --show-tabs           display TAB characters as ^I
  -s, --squeeze-blank       suppress repeated empty output lines
  -t, --t                   equivalent to -vT
```

### date - Print the current date and time
```bash
omni date [+FORMAT] [flags]
      --iso-8601            output date/time in ISO 8601 format
  -u, --utc                 print Coordinated Universal Time (UTC)
```

### dirname - Strip last component from file name
```bash
omni dirname [path...]
```

### echo - Display a line of text
```bash
omni echo [STRING]... [flags]
  -e, --escape              enable interpretation of backslash escapes
  -E, --no-escape           disable interpretation of backslash escapes (default)
  -n, --no-newline          do not output the trailing newline
```

### ls - List directory contents
```bash
omni ls [file...] [flags]
  -a, --all                 do not ignore entries starting with .
  -A, --almost-all          do not list implied . and ..
  -F, --classify            append indicator (*/=>@|) to entries
  -d, --directory           list directories themselves, not their contents
  -H, --human-readable      with -l, print sizes in human readable format
  -i, --inode               print the index number of each file
  -l, --long                use a long listing format
  -U, --no-sort             do not sort; list entries in directory order
  -1, --one                 list one file per line
  -R, --recursive           list subdirectories recursively
  -r, --reverse             reverse order while sorting
  -S, --size                sort by file size, largest first
  -t, --time                sort by modification time, newest first
```

### path - Path manipulation utilities
```bash
omni path
```

### pwd - Print working directory
```bash
omni pwd
```

### realpath - Print the resolved path
```bash
omni realpath [path...]
```

### testcheck - Check test coverage for Go packages
```bash
omni testcheck [directory] [flags]
  -a, --all                 show all packages (default shows only missing)
  -s, --summary             show only summary
  -v, --verbose             show test file names
```

## File Operations

### chmod - Change file mode bits
```bash
omni chmod [OPTION]... MODE[,MODE]... FILE... [flags]
  -c, --changes             like verbose but report only when a change is made
  -R, --recursive           change files and directories recursively
      --reference string    use RFILE's mode instead of MODE values
  -f, --silent              suppress most error messages
  -v, --verbose             output a diagnostic for every file processed
```

### chown - Change file owner and group
```bash
omni chown [OPTION]... OWNER[:GROUP] FILE... [flags]
  -c, --changes             like verbose but report only when a change is made
  -h, --no-dereference      affect symbolic links instead of referenced file
      --preserve-root       fail to operate recursively on '/'
  -R, --recursive           operate on files and directories recursively
      --reference string    use RFILE's owner and group
  -f, --silent              suppress most error messages
  -v, --verbose             output a diagnostic for every file processed
```

### copy - Alias for cp
```bash
omni copy
```

### cp - Copy files and directories
```bash
omni cp [source...] [destination]
```

### ln - Make links between files
```bash
omni ln [OPTION]... TARGET LINK_NAME [flags]
  -b, --backup              make a backup of each existing destination file
  -f, --force               remove existing destination files
  -n, --no-dereference      treat LINK_NAME as a normal file if it is a symlink
  -r, --relative            create symbolic links relative to link location
  -s, --symbolic            make symbolic links instead of hard links
  -v, --verbose             print name of each linked file
```

### mkdir - Create directories
```bash
omni mkdir [directory...] [flags]
  -p, --parents             no error if existing, make parent directories as needed
```

### move - Alias for mv
```bash
omni move
```

### mv - Move (rename) files
```bash
omni mv [source...] [destination]
```

### readlink - Print resolved symbolic links or canonical file names
```bash
omni readlink [OPTION]... FILE... [flags]
  -f, --canonicalize        canonicalize by following every symlink
  -e, --canonicalize-existing  canonicalize, all components must exist
  -m, --canonicalize-missing  canonicalize without requirements on existence
  -n, --no-newline          do not output the trailing delimiter
  -q, --quiet               suppress most error messages
  -s, --silent              suppress most error messages
  -v, --verbose             report error messages
  -z, --zero                end each output line with NUL, not newline
```

### remove - Alias for rm
```bash
omni remove
```

### rm - Remove files or directories
```bash
omni rm [file...] [flags]
  -f, --force               ignore nonexistent files and arguments, never prompt
      --no-preserve-root    do not treat protected paths specially (dangerous)
  -r, --recursive           remove directories and their contents recursively
```

### rmdir - Remove empty directories
```bash
omni rmdir [directory...] [flags]
      --no-preserve-root    do not treat protected paths specially (dangerous)
```

### stat - Display file or file system status
```bash
omni stat [file...]
```

### touch - Update the access and modification times of each FILE to the current time
```bash
omni touch [file...]
```

## Text Processing

### awk - Pattern scanning and processing language
```bash
omni awk [OPTION]... 'program' [FILE]... [flags]
  -v, --assign stringSlice  assign value to variable (var=value)
  -F, --field-separator string  use FS for the input field separator
```

### column - Columnate lists
```bash
omni column [OPTION]... [FILE]... [flags]
  -c, --columns int         output width in characters
  -x, --fillrows            fill rows before columns
  -H, --headers string      column headers (comma-separated)
  -J, --json                output as JSON
  -o, --output-separator string  output separator for table mode
  -R, --right               right-align columns
  -s, --separator string    delimiter characters for -t option
  -t, --table               determine column count based on input
```

### cut - Remove sections from each line of files
```bash
omni cut [OPTION]... [FILE]... [flags]
  -b, --bytes string        select only these bytes
  -c, --characters string   select only these characters
      --complement          complement the set of selected bytes, characters or fields
  -d, --delimiter string    use DELIM instead of TAB for field delimiter
  -f, --fields string       select only these fields
  -s, --only-delimited      do not print lines not containing delimiters
      --output-delimiter string  use STRING as the output delimiter
```

### egrep - Print lines that match patterns (extended regexp)
```bash
omni egrep [options] PATTERN [FILE...] [flags]
  -C, --context int         print NUM lines of context
  -c, --count               only print a count of matching lines
  -l, --files-with-matches  only print FILE names with matches
  -i, --ignore-case         ignore case distinctions
  -v, --invert-match        select non-matching lines
  -n, --line-number         prefix each line with line number
  -o, --only-matching       show only matched parts
  -q, --quiet               suppress all normal output
```

### fgrep - Print lines that match patterns (fixed strings)
```bash
omni fgrep [options] PATTERN [FILE...] [flags]
  -C, --context int         print NUM lines of context
  -c, --count               only print a count of matching lines
  -l, --files-with-matches  only print FILE names with matches
  -i, --ignore-case         ignore case distinctions
  -v, --invert-match        select non-matching lines
  -n, --line-number         prefix each line with line number
  -o, --only-matching       show only matched parts
  -q, --quiet               suppress all normal output
```

### fold - Wrap each input line to fit in specified width
```bash
omni fold [OPTION]... [FILE]... [flags]
  -b, --bytes               count bytes rather than columns
  -s, --spaces              break at spaces
  -w, --width int           use WIDTH columns instead of 80
```

### grep - Print lines that match patterns
```bash
omni grep [options] PATTERN [FILE...] [flags]
  -A, --after-context int   print NUM lines of trailing context
  -B, --before-context int  print NUM lines of leading context
  -C, --context int         print NUM lines of output context
  -c, --count               only print a count of matching lines per FILE
  -E, --extended-regexp     interpret PATTERN as an extended regular expression
  -l, --files-with-matches  only print FILE names containing matches
  -L, --files-without-match  only print FILE names not containing matches
  -F, --fixed-strings       interpret PATTERN as fixed strings
  -i, --ignore-case         ignore case distinctions in patterns and data
  -v, --invert-match        select non-matching lines
  -n, --line-number         prefix each line of output with line number
  -x, --line-regexp         match only whole lines
  -m, --max-count int       stop after NUM matches
      --no-filename         suppress the file name prefix on output
  -o, --only-matching       show only nonempty parts of lines that match
  -q, --quiet               suppress all normal output
  -r, --recursive           search directories recursively
  -H, --with-filename       print file name with output lines
  -w, --word-regexp         match only whole words
```

### head - Output the first part of files
```bash
omni head [option]... [file]... [flags]
  -c, --bytes int           print the first NUM bytes of each file
  -n, --lines int           print the first NUM lines instead of the first 10
  -q, --quiet               never print headers giving file names
  -v, --verbose             always print headers giving file names
```

### join - Join lines of two files on a common field
```bash
omni join [OPTION]... FILE1 FILE2 [flags]
  -1, --1 int               join on this FIELD of file 1
  -2, --2 int               join on this FIELD of file 2
  -a, --a int               also print unpairable lines from file FILENUM (1 or 2)
  -e, --e string            replace missing fields with EMPTY
  -i, --i                   ignore case when comparing fields
  -t, --t string            use CHAR as input and output field separator
  -v, --v int               print only unpairable lines from file FILENUM (1 or 2)
```

### nl - Number lines of files
```bash
omni nl [OPTION]... [FILE]... [flags]
  -b, --body-numbering string  use STYLE for numbering body lines
  -i, --line-increment int  line number increment
  -n, --number-format string  insert line numbers according to FORMAT
  -s, --number-separator string  add STRING after line number
  -w, --number-width int    use N columns for line numbers
  -v, --starting-line-number int  first line number
```

### paste - Merge lines of files
```bash
omni paste [OPTION]... [FILE]... [flags]
  -d, --delimiters string   reuse characters from LIST instead of TABs
  -s, --serial              paste one file at a time instead of in parallel
  -z, --zero-terminated     line delimiter is NUL, not newline
```

### rg - Recursively search for a pattern (ripgrep-style)
```bash
omni rg [OPTIONS] PATTERN [PATH...] [flags]
  -A, --after-context int   show N lines after match
  -B, --before-context int  show N lines before match
  -b, --byte-offset         show byte offset of each line (not yet implemented)
      --color string        when to use colors: auto, always, never
      --colors stringSlice  custom color specification (e.g., 'path:fg:magenta')
      --column              show column numbers
  -C, --context int         show N lines before and after match
  -c, --count               only show count of matches per file
  -l, --files-with-matches  only show file names with matches
  -F, --fixed-strings       treat pattern as literal string
  -L, --follow              follow symbolic links
  -g, --glob stringSlice    include/exclude files matching GLOB (prefix with ! to exclude)
      --hidden              search hidden files and directories
  -i, --ignore-case         case insensitive search
  -v, --invert-match        show non-matching lines
      --json-stream         output results as streaming NDJSON (one JSON object per line)
  -n, --line-number         show line numbers
  -m, --max-count int       limit matches per file
      --max-depth int       limit directory traversal depth
  -U, --multiline           enable multiline matching
  -H, --no-heading          don't group matches by file name
      --no-ignore           don't respect gitignore files
  -o, --only-matching       show only matching part of line
      --passthru            show all lines, highlighting matches
  -q, --quiet               quiet mode, exit on first match
  -r, --replace string      replace matches with STRING
  -S, --smart-case          smart case (insensitive if pattern is all lowercase)
      --stats               show search statistics
  -j, --threads int         number of worker threads (default: CPU count)
      --trim                trim leading/trailing whitespace from each line
  -t, --type stringSlice    only search files of TYPE (go, js, py, etc.)
  -T, --type-not stringSlice  exclude files of TYPE
  -w, --word-regexp         only match whole words
```

### sed - Stream editor for filtering and transforming text
```bash
omni sed [OPTION]... {script} [FILE]... [flags]
  -e, --expression stringSlice  add the script to the commands to be executed
  -i, --in-place            edit files in place
      --in-place-suffix string  backup suffix for in-place edit
  -n, --quiet               suppress automatic printing of pattern space
  -r, --r                   use extended regular expressions (alias)
  -E, --regexp-extended     use extended regular expressions
```

### sort - Sort lines of text files
```bash
omni sort [option]... [file]... [flags]
  -c, --check               check for sorted input; do not sort
  -d, --dictionary-order    consider only blanks and alphanumeric characters
  -t, --field-separator string  use SEP instead of non-blank to blank transition
  -f, --ignore-case         fold lower case to upper case characters
  -b, --ignore-leading-blanks  ignore leading blanks
  -k, --key string          sort via a key
  -n, --numeric-sort        compare according to string numerical value
  -o, --output string       write result to FILE instead of standard output
  -r, --reverse             reverse the result of comparisons
  -s, --stable              stabilize sort by disabling last-resort comparison
  -u, --unique              with -c, check for strict ordering; without -c, output only the first of an equal run
```

### tac - Concatenate and print files in reverse
```bash
omni tac [OPTION]... [FILE]... [flags]
  -b, --before              attach the separator before instead of after
  -r, --regex               interpret the separator as a regular expression
  -s, --separator string    use STRING as the separator instead of newline
```

### tail - Output the last part of files
```bash
omni tail [option]... [file]... [flags]
  -c, --bytes int           output the last NUM bytes
  -f, --follow              output appended data as the file grows
  -n, --lines int           output the last NUM lines, instead of the last 10
  -q, --quiet               never output headers giving file names
      --sleep-interval duration  with -f, sleep for approximately N seconds between iterations
  -v, --verbose             always output headers giving file names
```

### tr - Translate or delete characters
```bash
omni tr [OPTION]... SET1 [SET2] [flags]
  -c, --complement          use the complement of SET1
  -d, --delete              delete characters in SET1, do not translate
  -s, --squeeze-repeats     replace repeated characters with single occurrence
  -t, --truncate-set1       first truncate SET1 to length of SET2
```

### uniq - Report or omit repeated lines
```bash
omni uniq [option]... [input [output]] [flags]
  -D, --all-repeated        print all duplicate lines
  -w, --check-chars int     compare no more than N characters in lines
  -c, --count               prefix lines by the number of occurrences
  -i, --ignore-case         ignore differences in case when comparing
  -d, --repeated            only print duplicate lines, one for each group
  -s, --skip-chars int      avoid comparing the first N characters
  -f, --skip-fields int     avoid comparing the first N fields
  -u, --unique              only print unique lines
  -z, --zero-terminated     line delimiter is NUL, not newline
```

### wc - Print newline, word, and byte counts for each file
```bash
omni wc [option]... [file]... [flags]
  -c, --bytes               print the byte counts
  -m, --chars               print the character counts
  -l, --lines               print the newline counts
  -L, --max-line-length     print the maximum display width
  -w, --words               print the word counts
```

## System Information

### df - Report file system disk space usage
```bash
omni df [OPTION]... [FILE]... [flags]
  -B, --block-size int64    scale sizes by SIZE before printing them
  -x, --exclude-type string  exclude file systems of type TYPE
  -H, --human-readable      print sizes in human readable format
  -i, --inodes              list inode information instead of block usage
  -l, --local               limit listing to local file systems
  -P, --portability         use the POSIX output format
      --total               produce a grand total
  -t, --type string         limit listing to file systems of type TYPE
```

### du - Estimate file space usage
```bash
omni du [OPTION]... [FILE]... [flags]
  -a, --all                 write counts for all files, not just directories
      --apparent-size       print apparent sizes, rather than disk usage
  -B, --block-size int64    scale sizes by SIZE before printing them
  -b, --bytes               equivalent to --apparent-size --block-size=1
  -H, --human-readable      print sizes in human readable format
  -d, --max-depth int       print total for directory only if N or fewer levels deep
  -0, --null                end each output line with NUL, not newline
  -x, --one-file-system     skip directories on different file systems
  -s, --summarize           display only a total for each argument
  -c, --total               produce a grand total
```

### env - Print environment variables
```bash
omni env [NAME...] [flags]
  -i, --ignore-environment  start with an empty environment
  -0, --null                end each output line with NUL, not newline
  -u, --unset string        remove variable from the environment
```

### free - Display amount of free and used memory in the system
```bash
omni free [OPTION]... [flags]
  -b, --bytes               show output in bytes
  -g, --gibibytes           show output in gibibytes
  -H, --human               show human-readable output
  -k, --kibibytes           show output in kibibytes
  -m, --mebibytes           show output in mebibytes
  -t, --total               show total for RAM + swap
  -w, --wide                wide output
```

### id - Print user and group information
```bash
omni id [OPTION]... [USER] [flags]
  -g, --group               print only the effective group ID
  -G, --groups              print all group IDs
  -n, --name                print a name instead of a number
  -r, --real                print the real ID instead of the effective ID
  -u, --user                print only the effective user ID
```

### kill - Send a signal to a process
```bash
omni kill [OPTION]... PID... [flags]
  -l, --list                list signal names
  -s, --signal string       specify the signal to be sent
  -v, --verbose             report successful signals
```

### ps - Report a snapshot of current processes
```bash
omni ps [OPTION]... [flags]
  -a, --all                 show processes for all users
  -f, --full                full-format listing
      --go                  show only Go processes
  -l, --long                long format
      --no-headers          don't print header line
  -p, --pid int             show process with specified PID
      --sort string         sort by column (pid, cpu, mem, time)
  -u, --user string         show processes for specified user
```

### time - Time a simple command or give resource usage
```bash
omni time
```

### uname - Print system information
```bash
omni uname [flags]
  -a, --all                 print all information
  -i, --hardware-platform   print the hardware platform
  -s, --kernel-name         print the kernel name
  -r, --kernel-release      print the kernel release
  -v, --kernel-version      print the kernel version
  -m, --machine             print the machine hardware name
  -n, --nodename            print the network node hostname
  -o, --operating-system    print the operating system
  -p, --processor           print the processor type
```

### uptime - Tell how long the system has been running
```bash
omni uptime [OPTION]... [flags]
  -p, --pretty              show uptime in pretty format
  -s, --since               system up since
```

### whoami - Print effective username
```bash
omni whoami
```

## Process (runtime-aware)

### gops - List and signal running Go processes
```bash
omni gops [flags]
  -a, --all                 include the omni process itself
  -j, --json                output as JSON
```

### javaps - List and signal running Java (JVM) processes
```bash
omni javaps [flags]
  -a, --all                 include the omni process itself
  -j, --json                output as JSON
```

### nodeps - List and signal running Node.js processes
```bash
omni nodeps [flags]
  -a, --all                 include the omni process itself
  -j, --json                output as JSON
```

### pyps - List and signal running Python processes
```bash
omni pyps [flags]
  -a, --all                 include the omni process itself
  -j, --json                output as JSON
```

## Flow Control

### pipe - Chain omni commands without shell pipes
```bash
omni pipe {CMD}, {CMD}, ... | CMD | CMD [flags]
  -s, --sep string          command separator
      --var string          variable name for output substitution (default: OUT)
  -v, --verbose             show intermediate results
```

### watch - Execute a program periodically, showing output fullscreen
```bash
omni watch [OPTION]... COMMAND [flags]
  -b, --beep                beep if command has a non-zero exit
  -g, --chgexit             exit when output changes
  -c, --color               interpret ANSI color sequences
  -d, --differences         highlight differences between updates
  -e, --errexit             exit if command has a non-zero exit
  -n, --interval float64    seconds to wait between updates
  -t, --no-title            turn off header
      --only-changes        only display output when it changes
  -p, --precise             attempt run command in precise intervals
```

### xargs - Build and execute command lines from standard input
```bash
omni xargs [OPTION]... [COMMAND [INITIAL-ARGS]] [flags]
  -I, --I string            replace occurrences of REPLACE-STR
  -d, --delimiter string    input items are separated by DELIM
  -n, --max-args int        use at most MAX arguments per command line
  -P, --max-procs int       run at most MAX processes at a time
  -r, --no-run-if-empty     if there are no arguments, do not run
  -0, --null                input items are separated by a null character
  -t, --verbose             print commands before executing them
```

### yes - Output a string repeatedly until killed
```bash
omni yes [STRING]...
```

## Archive & Compression

### tar - Create, extract, or list archive files
```bash
omni tar [OPTION]... [FILE]... [flags]
  -c, --create              create a new archive
  -C, --directory string    change to directory DIR
  -x, --extract             extract files from an archive
  -f, --file string         use archive file ARCHIVE
  -z, --gzip                filter through gzip
      --json                output as JSON (for list mode)
  -t, --list                list the contents of an archive
      --strip-components int  strip N leading path components
  -v, --verbose             verbosely list files processed
```

### unzip - Extract files from a zip archive
```bash
omni unzip [OPTION]... ZIPFILE [flags]
  -d, --directory string    extract files into directory
      --json                output as JSON (for list mode)
  -l, --list                list contents without extracting
      --strip-components int  strip N leading path components
  -v, --verbose             verbose output
```

### zip - Package and compress files into a zip archive
```bash
omni zip [OPTION]... ZIPFILE FILE... [flags]
  -C, --directory string    change to directory before adding
  -r, --recursive           recurse into directories
  -v, --verbose             verbose output
```

## Hash & Encoding

### base32 - Base32 encode or decode data
```bash
omni base32 [OPTION]... [FILE] [flags]
  -d, --decode              decode data
  -w, --wrap int            wrap encoded lines after N characters (0 = no wrap)
```

### base58 - Base58 encode or decode data (Bitcoin alphabet)
```bash
omni base58 [OPTION]... [FILE] [flags]
  -d, --decode              decode data
```

### base64 - Base64 encode or decode data
```bash
omni base64 [OPTION]... [FILE] [flags]
  -d, --decode              decode data
  -i, --ignore-garbage      ignore non-alphabet characters when decoding
  -w, --wrap int            wrap encoded lines after N characters (0 = no wrap)
```

### hash - Compute and check file hashes
```bash
omni hash [OPTION]... [FILE]... [flags]
  -a, --algorithm string    hash algorithm (md5, sha1, sha256, sha512, crc32, crc64)
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
  -r, --recursive           hash files recursively
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### md5sum - Compute and check MD5 message digest
```bash
omni md5sum [OPTION]... [FILE]... [flags]
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### sha256sum - Compute and check SHA256 message digest
```bash
omni sha256sum [OPTION]... [FILE]... [flags]
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### sha512sum - Compute and check SHA512 message digest
```bash
omni sha512sum [OPTION]... [FILE]... [flags]
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### xxd - Make a hex dump or reverse it
```bash
omni xxd [OPTIONS] [FILE] [flags]
  -b, --bits                binary digit dump (bits instead of hex)
  -c, --cols int            format <cols> octets per line (default 16)
  -g, --groupsize int       separate output with <bytes> spaces (default 2)
  -i, --include             output in C include file style
  -l, --len int             stop after <len> octets
  -p, --plain               output plain hex dump (no addresses or ASCII)
  -r, --reverse             reverse operation: convert hex dump to binary
  -s, --seek int            start at <seek> bytes offset
  -u, --uppercase           use uppercase hex letters
```

## Data Processing

### dotenv - Load environment variables from .env files
```bash
omni dotenv [OPTION]... [FILE]... [flags]
  -x, --expand              expand variables in values
  -e, --export              output as export statements
  -q, --quiet               suppress warnings
  -s, --shell string        target shell (auto, bash, zsh, fish, powershell, cmd, nushell)
```

### jq - Command-line JSON processor
```bash
omni jq [OPTION]... FILTER [FILE]... [flags]
  -c, --compact-output      compact output
  -n, --null-input          don't read any input
  -r, --raw-output          output raw strings
  -s, --slurp               read all inputs into array
  -S, --sort-keys           sort object keys
      --tab                 use tabs for indentation
```

### yq - Command-line YAML processor
```bash
omni yq [OPTION]... FILTER [FILE]... [flags]
  -c, --compact-output      compact output
  -I, --indent int          indentation level
  -n, --null-input          don't read any input
  -o, --output-format string  output format (yaml or json)
  -r, --raw-output          output raw strings
```

## Security & Random

### attest - Generate a signed SLSA provenance attestation (DSSE/in-toto)
```bash
omni attest [OPTION]... --artifact FILE [flags]
  -a, --artifact string     artifact to attest
      --builder-id string   builder.id (ADR-0009-allowed; default: local)
      --from-env            populate provenance from GITHUB_* env vars
  -k, --key string          secret key file (*.key)
  -o, --out string          output envelope path (default: stdout)
      --predicate string    pre-built predicate JSON file
      --predicate-type string  predicate type (only: slsa-provenance)
```

### decrypt - Decrypt data using AES-256-GCM
```bash
omni decrypt [OPTION]... [FILE] [flags]
  -a, --armor               input is ASCII armored (base64)
  -b, --base64              input is base64 (same as -a)
  -i, --iterations int      PBKDF2 iterations
  -k, --key-file string     use key file for decryption
  -o, --output string       write output to file
  -p, --password string     password for decryption
  -P, --password-file string  read password from file
```

### encrypt - Encrypt data using AES-256-GCM
```bash
omni encrypt [OPTION]... [FILE] [flags]
  -a, --armor               ASCII armor (base64) output
  -b, --base64              base64 output (same as -a)
  -i, --iterations int      PBKDF2 iterations
  -k, --key-file string     use key file for encryption
  -o, --output string       write output to file
  -p, --password string     password for encryption
  -P, --password-file string  read password from file
```

### random - Generate random values
```bash
omni random [OPTION]... [flags]
  -c, --charset string      custom character set
  -n, --count int           number of values to generate
  -l, --length int          length of random strings
      --max int64           maximum value for integers
      --min int64           minimum value for integers
  -s, --separator string    separator between values
  -t, --type string         type: int, float, string, alpha, hex, password, bytes, custom
```

### reprocheck - Fail if any A/B build artifact pair differs (reproducible-build gate)
```bash
omni reprocheck --a FILE [--a FILE...] --b FILE [--b FILE...] [flags]
      --a stringArray       artifact path from build A (repeatable)
      --b stringArray       artifact path from build B (repeatable)
```

### sbom - Generate an SBOM (SPDX 2.3 or CycloneDX 1.5) for a Go module dir or binary
```bash
omni sbom [OPTION]... PATH [flags]
      --format string       output format: spdx|cyclonedx
      --from string         source kind: auto|module|binary
  -k, --key string          secret key path for --sign
      --out string          write to FILE instead of stdout
      --sign                sign --out with a minisign key
      --source-date string  fixed RFC-3339 creation timestamp (default: epoch)
      --validate            validate emitted document against the upstream schema (requires -tags omni_sbomvalidate)
```

### scan - Scan an SBOM against a signed OSV vulnerability database
```bash
omni scan <sbom>
```

### uuid - Generate random UUIDs
```bash
omni uuid [OPTION]... [flags]
  -n, --count int           generate N UUIDs
  -x, --no-dashes           output without dashes
  -u, --upper               output in uppercase
  -v, --version int         UUID version (4 or 7)
```

## TUI Pagers

### less - View file contents with scrolling
```bash
omni less [OPTION]... [FILE] [flags]
  -N, --LINE-NUMBERS        show line numbers
  -S, --chop-long-lines     truncate long lines
  -i, --ignore-case         case-insensitive search
  -X, --no-init             don't clear screen on start
  -F, --quit-if-one-screen  quit if content fits on one screen
  -R, --raw-control-chars   show raw control characters
```

### more - View file contents page by page
```bash
omni more [OPTION]... [FILE] [flags]
  -n, --line-numbers        show line numbers
```

## Comparison

### diff - Compare files line by line
```bash
omni diff [OPTION]... FILE1 FILE2 [flags]
  -q, --brief               report only when files differ
      --color               colorize the output
  -B, --ignore-blank-lines  ignore changes where lines are all blank
  -i, --ignore-case         ignore case differences
  -b, --ignore-space-change  ignore changes in amount of white space
      --json                compare as JSON files
  -r, --recursive           recursively compare subdirectories
  -y, --side-by-side        output in two columns
      --suppress-common-lines  do not output common lines in side-by-side
  -u, --unified int         output NUM lines of unified context
  -W, --width int           output at most NUM columns
```

## Tooling

### cmdtree - Display command tree visualization
```bash
omni cmdtree [flags]
  -b, --brief               Show compact tree with short descriptions only
  -c, --command string      Show details for a specific command only
  -v, --verbose             Show full details for all commands (default)
```

### lint - Check Taskfiles for portability issues
```bash
omni lint [OPTION]... [FILE|DIR]... [flags]
      --fix                 auto-fix issues where possible
  -q, --quiet               only show errors, not warnings
      --strict              enable strict mode (more warnings become errors)
```

### logger - Configure omni command logging
```bash
omni logger [flags]
  -d, --disable             Disable logging (unset environment variables)
  -p, --path string         Path to the log file
  -s, --status              Show current logging status
  -v, --viewer              View all log files sorted by time
```

## Other Commands

### aicontext - Generate AI context for coding agents
```bash
omni aicontext [flags]
  -c, --category string     filter category
  -j, --json                JSON output
      --no-structure        omit project structure
  -o, --output string       write to file
```

### arch - Print machine architecture
```bash
omni arch
```

### aws - AWS CLI operations
```bash
omni aws
```

### banner - Generate ASCII art text banners
```bash
omni banner [TEXT] [flags]
  -f, --font string         font name
  -l, --list                list available fonts
  -w, --width int           max output width (0 = unlimited)
```

### bbolt - BoltDB database management
```bash
omni bbolt
```

### brdoc - Brazilian document utilities (CPF, CNPJ)
```bash
omni brdoc
```

### buf - Protocol buffer utilities (lint, format, compile, generate)
```bash
omni buf
```

### bunzip2 - Decompress bzip2 files
```bash
omni bunzip2 [OPTION]... [FILE]... [flags]
  -f, --force               force overwrite
  -k, --keep                keep original files
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### bzcat - Decompress and print bzip2 files
```bash
omni bzcat [FILE]...
```

### bzip2 - Decompress bzip2 files
```bash
omni bzip2 [OPTION]... [FILE]... [flags]
  -d, --decompress          decompress
  -f, --force               force overwrite
  -k, --keep                keep original files
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### case - Text case conversion utilities
```bash
omni case
```

### cloud - Cloud profile management
```bash
omni cloud
```

### cmp - Compare two files byte by byte
```bash
omni cmp [OPTION]... FILE1 FILE2 [flags]
  -n, --bytes int64         compare at most LIMIT bytes
  -i, --ignore-initial int64  skip first SKIP bytes
  -b, --print-bytes         print differing bytes
  -s, --silent              suppress all output
  -l, --verbose             output byte numbers and values
```

### comm - Compare two sorted files line by line
```bash
omni comm [OPTION]... FILE1 FILE2 [flags]
  -1, --1                   suppress column 1
  -2, --2                   suppress column 2
  -3, --3                   suppress column 3
      --check-order         check input is sorted
      --nocheck-order       do not check input order
      --output-delimiter string  use STR as output delimiter
  -z, --zero-terminated     line delimiter is NUL
```

### crc32sum - Compute and check CRC32 checksums
```bash
omni crc32sum [OPTION]... [FILE]... [flags]
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### crc64sum - Compute and check CRC64 checksums
```bash
omni crc64sum [OPTION]... [FILE]... [flags]
  -b, --binary              read in binary mode
  -c, --check               read checksums from FILE and check them
      --quiet               don't print OK for verified files
      --status              don't output anything, use status code
  -w, --warn                warn about improperly formatted lines
```

### cron - Parse and explain cron expressions
```bash
omni cron EXPRESSION [flags]
      --next int            show next N scheduled runs
      --validate            only validate the expression
```

### css - CSS utilities (format, minify, validate)
```bash
omni css [FILE] [flags]
  -i, --indent string       indentation string
      --sort-props          sort properties alphabetically
      --sort-rules          sort selectors alphabetically
```

### csv - CSV utilities (convert to/from JSON)
```bash
omni csv
```

### curl - HTTP client with httpie-like syntax
```bash
omni curl [METHOD] URL [ITEM...] [flags]
  -d, --data string         request body data
  -f, --form                send as form data instead of JSON
  -H, --header stringArray  custom header (can be used multiple times)
  -k, --insecure            skip TLS verification
      --json                output response as structured JSON
  -L, --location            follow redirects
  -t, --timeout int         request timeout in seconds
  -v, --verbose             show request/response details
```

### dd - Convert and copy a file
```bash
omni dd [OPERAND]...
```

### exec - Run external commands with credential pre-flight checks
```bash
omni exec <command> [args...] [flags]
      --dry-run             Show credential checks without executing
  -f, --force               Skip credential checks, execute immediately
      --no-prompt           Don't prompt, just warn and proceed
      --strict              Abort if any credentials are missing (CI mode)
```

### exist - Check if files, directories, commands, env vars, processes, or ports exist
```bash
omni exist
```

### file - Determine file type
```bash
omni file [OPTION]... FILE... [flags]
  -b, --brief               do not prepend filenames
  -i, --mime                output MIME type
  -L, --no-dereference      don't follow symlinks
  -F, --separator string    use string as separator
```

### find - Search for files in a directory hierarchy
```bash
omni find [path...] [expression] [flags]
      --amin string         access time [+-]N minutes
      --atime string        access time [+-]N days
      --empty               file is empty
      --executable          file is executable
      --iname string        case insensitive name match
      --ipath string        case insensitive path match
      --iregex string       case insensitive regex
      --maxdepth int        maximum depth (0=unlimited)
      --mindepth int        minimum depth
      --mmin string         modification time [+-]N minutes
      --mtime string        modification time [+-]N days
      --name string         file name matches pattern
      --not                 negate next test
      --path string         path matches pattern
  -0, --print0              print with null terminator
      --readable            file is readable
      --regex string        path matches regex
      --size string         file size [+-]N[ckMG]
      --type string         file type (f=file, d=dir, l=link)
      --writable            file is writable
```

### for - Loop and execute commands
```bash
omni for
```

### gbc - Git branch clean (alias)
```bash
omni gbc [flags]
      --dry-run             Show branches that would be deleted
```

### gh - GitHub CLI shortcuts
```bash
omni gh
```

### git - Git shortcuts and hacks
```bash
omni git
```

### gqc - Git quick commit (alias)
```bash
omni gqc [flags]
  -a, --all                 Stage all changes before commit
  -m, --message string      Commit message (required)
```

### gunzip - Decompress gzip files
```bash
omni gunzip [OPTION]... [FILE]... [flags]
  -f, --force               force overwrite
  -k, --keep                keep original files
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### gzip - Compress or decompress files
```bash
omni gzip [OPTION]... [FILE]... [flags]
  -9, --best int            compress better
  -d, --decompress          decompress
  -1, --fast int            compress faster
  -f, --force               force overwrite
  -k, --keep                keep original files
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### hex - Hexadecimal encoding and decoding utilities
```bash
omni hex
```

### html - HTML utilities (format, encode, decode)
```bash
omni html
```

### json - JSON utilities (format, minify, validate)
```bash
omni json
```

### jwt - JWT (JSON Web Token) utilities
```bash
omni jwt
```

### kconfig - Show kubeconfig info
```bash
omni kconfig
```

### kcs - Switch kubectl context
```bash
omni kcs [context]
```

### kdebug - Debug pod with ephemeral container
```bash
omni kdebug <pod> [flags]
      --image string        Debug container image (default: busybox)
  -n, --namespace string    Namespace
```

### kdp - Delete pods by selector
```bash
omni kdp <selector> [flags]
      --force               Force delete
  -n, --namespace string    Namespace
```

### kdrain - Drain node for maintenance
```bash
omni kdrain <node> [flags]
      --delete-emptydir     Delete emptydir data
      --ignore-daemonsets   Ignore daemonsets
```

### keb - Exec into pod with bash
```bash
omni keb <pod> [flags]
  -c, --container string    Container name
  -n, --namespace string    Namespace
```

### kga - Get all resources in namespace
```bash
omni kga [flags]
  -A, --all-namespaces      All namespaces
  -n, --namespace string    Namespace
```

### kge - Get events sorted by time
```bash
omni kge [flags]
  -A, --all-namespaces      All namespaces
  -n, --namespace string    Namespace
```

### klf - Follow pod logs with timestamps
```bash
omni klf <pod> [flags]
  -c, --container string    Container name
  -n, --namespace string    Namespace
      --tail int            Lines to show from end of logs
```

### kns - Switch default namespace
```bash
omni kns [namespace]
```

### kpf - Quick port forward
```bash
omni kpf <pod|svc/name> <local:remote> [flags]
  -n, --namespace string    Namespace
```

### krr - Rollout restart deployment
```bash
omni krr <deployment> [flags]
  -n, --namespace string    Namespace
```

### krun - Run a one-off pod
```bash
omni krun <name> --image=<image> [-- command] [flags]
      --image string        Container image (required)
  -n, --namespace string    Namespace
```

### kscale - Scale deployment
```bash
omni kscale <deployment> <replicas> [flags]
  -n, --namespace string    Namespace
```

### ksuid - Generate K-Sortable Unique IDentifiers
```bash
omni ksuid [OPTION]... [flags]
  -n, --count int           generate N KSUIDs
```

### ktn - Top nodes by resource usage
```bash
omni ktn [flags]
      --sort-by string      Sort by (cpu or memory)
```

### ktp - Top pods by resource usage
```bash
omni ktp [flags]
  -A, --all-namespaces      All namespaces
  -n, --namespace string    Namespace
      --sort-by string      Sort by (cpu or memory)
```

### kubectl - Kubernetes CLI
```bash
omni kubectl
```

### kwp - Watch pods continuously
```bash
omni kwp [flags]
  -A, --all-namespaces      All namespaces
  -n, --namespace string    Namespace
  -l, --selector string     Label selector
```

### loc - Count lines of code by language
```bash
omni loc [PATH]... [flags]
  -e, --exclude stringSlice  directories to exclude
      --hidden              include hidden files
```

### lsof - List open files and network connections
```bash
omni lsof [OPTIONS] [flags]
  -c, --command string      filter by command name prefix
  -e, --established         show only established connections
  -4, --ipv4                show only IPv4
  -6, --ipv6                show only IPv6
  -l, --listen              show only listening sockets
  -i, --network             show network connections only
  -n, --no-headers          don't print headers
  -p, --pid int             show files for specific process ID
      --port int            filter by port number (use with -i)
  -t, --tcp                 show TCP connections only
  -U, --udp                 show UDP connections only
  -u, --user string         show files for specific user
```

### nanoid - Generate compact, URL-safe unique IDs
```bash
omni nanoid [OPTION]... [flags]
  -a, --alphabet string     custom alphabet
  -n, --count int           generate N NanoIDs
  -l, --length int          length of NanoID
```

### netstat - Display network connections (alias for ss)
```bash
omni netstat [OPTIONS] [flags]
  -a, --all                 display all sockets
  -l, --listening           display listening sockets only
  -n, --numeric             don't resolve service names
  -p, --processes           show process using socket
  -t, --tcp                 display TCP sockets
  -u, --udp                 display UDP sockets
```

### note - Quick note taking to JSON in your Documents folder
```bash
omni note [flags]
  -n, --limit int           show only last N notes (used with --list)
      --list                list saved notes instead of adding a new one
```

### pgrep - Find processes by name or pattern
```bash
omni pgrep [OPTIONS] PATTERN [flags]
  -c, --count               count matching processes
  -x, --exact               match exactly
  -f, --full                match against full command line
  -i, --ignore-case         case insensitive matching
  -n, --newest              select only the newest process
  -o, --oldest              select only the oldest process
  -P, --parent int          match only processes with parent PID
  -u, --user string         match only processes owned by user
```

### pipeline - Streaming text processing engine
```bash
omni pipeline STAGE [STAGE...] [flags]
  -f, --file string         input file (default: stdin)
  -v, --verbose             show stage names before processing
```

### pkill - Kill processes by name or pattern
```bash
omni pkill [OPTIONS] PATTERN [flags]
  -c, --count               count matching processes
  -x, --exact               match exactly
  -f, --full                match against full command line
  -i, --ignore-case         case insensitive matching
  -l, --list                list matching processes (pgrep mode)
  -n, --newest              select only the newest process
  -o, --oldest              select only the oldest process
  -P, --parent int          match only processes with parent PID
  -s, --signal string       signal to send (default: TERM)
  -u, --user string         match only processes owned by user
  -v, --verbose             verbose output
```

### printf - Format and print data
```bash
omni printf FORMAT [ARG...] [flags]
  -n, --no-newline          do not append a trailing newline
```

### project - Analyze project structure, dependencies, and health
```bash
omni project
```

### repo - Repository analysis tools
```bash
omni repo
```

### rev - Reverse lines characterwise
```bash
omni rev [FILE]...
```

### scaffold - Code scaffolding utilities
```bash
omni scaffold
```

### seq - Print a sequence of numbers
```bash
omni seq [OPTION]... LAST or seq [OPTION]... FIRST LAST or seq [OPTION]... FIRST INCREMENT LAST [flags]
  -w, --equal-width         equalize width with leading zeros
  -f, --format string       use printf style FORMAT
  -s, --separator string    use STRING to separate numbers
```

### shuf - Generate random permutations
```bash
omni shuf [OPTION]... [FILE] [flags]
  -e, --echo                treat each ARG as an input line
  -n, --head-count int      output at most COUNT lines
  -i, --input-range string  treat each number LO through HI as an input line
  -r, --repeat              output lines can be repeated
  -z, --zero-terminated     line delimiter is NUL
```

### sign - Create a detached minisign signature
```bash
omni sign [OPTION]... [FILE] [flags]
  -k, --key string          path to the secret key file
  -s, --sig string          output signature path (default: <FILE>.minisig)
  -t, --trusted-comment string  trusted comment embedded in the signature
      --untrusted-comment string  first-line comment of the signature file
```

### sleep - Delay for a specified amount of time
```bash
omni sleep NUMBER[SUFFIX]...
```

### snowflake - Generate Twitter Snowflake-style IDs
```bash
omni snowflake [OPTION]... [flags]
  -n, --count int           generate N Snowflake IDs
  -w, --worker int64        worker ID (0-1023)
```

### split - Split a file into pieces
```bash
omni split [OPTION]... [FILE [PREFIX]] [flags]
  -b, --bytes string        put SIZE bytes per output file
  -l, --lines int           put NUMBER lines per output file
  -d, --numeric-suffixes    use numeric suffixes
  -a, --suffix-length int   generate suffixes of length N
      --verbose             print diagnostic for each output file
```

### sql - SQL utilities (format, minify, validate)
```bash
omni sql [FILE] [flags]
  -i, --indent string       indentation string
  -u, --uppercase           uppercase keywords
```

### sqlite - SQLite database management
```bash
omni sqlite
```

### ss - Display socket statistics
```bash
omni ss [OPTIONS] [flags]
  -a, --all                 display all sockets
  -e, --extended            show extended socket info
  -4, --ipv4                display only IPv4 sockets
  -6, --ipv6                display only IPv6 sockets
  -l, --listening           display listening sockets only
      --no-header           don't print headers
  -n, --numeric             don't resolve service names
  -p, --processes           show process using socket
      --state string        filter by state (established, listen, time_wait, etc.)
  -s, --summary             print summary statistics
  -t, --tcp                 display TCP sockets
  -u, --udp                 display UDP sockets
  -x, --unix                display Unix sockets
```

### strings - Print the printable strings in files
```bash
omni strings [OPTION]... [FILE]... [flags]
  -n, --bytes int           minimum string length
  -t, --radix string        print offset (d/o/x)
```

### tagfixer - Fix and standardize Go struct tags
```bash
omni tagfixer [PATH] [flags]
  -a, --analyze             analyze mode - generate report only
  -c, --case string         target case type (camel, pascal, snake, kebab)
  -d, --dry-run             preview changes without writing
      --json                output as JSON
  -r, --recursive           process directories recursively
  -t, --tags string         comma-separated tag types to fix
  -v, --verbose             verbose output
```

### task - Run tasks defined in Taskfile.yml
```bash
omni task [TASK...] [flags]
      --allow-external      allow external (non-omni) commands
  -d, --dir string          working directory
      --dry-run             print commands without executing
  -f, --force               force run even if up-to-date
  -l, --list                list available tasks
  -s, --silent              suppress output
      --summary             show task summary
  -t, --taskfile string     path to Taskfile.yml
  -v, --verbose             verbose output
```

### terraform - Terraform CLI
```bash
omni terraform
```

### toml - TOML utilities
```bash
omni toml
```

### top - Display system processes sorted by resource usage
```bash
omni top [flags]
      --go                  show only Go processes
  -n, --num int             number of processes to show
      --sort string         sort by column: cpu, mem, pid
```

### tree - Display directory tree structure
```bash
omni tree [path] [flags]
  -a, --all                 show hidden files
      --compare stringSlice  compare two JSON tree snapshots
      --date                show modification dates
  -d, --depth int           maximum depth to scan (-1 for unlimited)
      --detect-moves        detect moved files when comparing (default true)
      --dirs-only           show only directories
      --hash                show SHA256 hash for files
  -i, --ignore string       patterns to ignore (comma-separated)
  -j, --json                output as JSON format
      --json-stream         streaming NDJSON output (one JSON object per line)
  -L, --level int           maximum depth level (alias for --depth)
      --max-files int       maximum number of files to scan (0 = unlimited)
      --max-hash-size int64  skip hashing files larger than N bytes (0 = unlimited)
      --no-color            disable colored output
      --no-dir-slash        don't add trailing slash to directory names
      --size                show file sizes
  -s, --stats               show statistics
  -t, --threads int         number of parallel workers (0 = auto, 1 = sequential)
```

### ulid - Generate Universally Unique Lexicographically Sortable Identifiers
```bash
omni ulid [OPTION]... [flags]
  -n, --count int           generate N ULIDs
  -l, --lower               output in lowercase
```

### unxz - Decompress xz files
```bash
omni unxz [OPTION]... [FILE]... [flags]
  -f, --force               force overwrite
  -k, --keep                keep original files
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### url - URL encoding and decoding utilities
```bash
omni url
```

### vault - HashiCorp Vault CLI
```bash
omni vault
```

### verify - Verify a detached minisign signature (fail-closed)
```bash
omni verify [OPTION]... FILE [flags]
      --bundle string       verify a Sigstore bundle (unsupported here; see contrib/sigstore-verify module)
      --cert-identity string  Sigstore certificate identity (sigstore-verify module only)
      --cert-oidc-issuer string  Sigstore OIDC issuer (sigstore-verify module only)
  -k, --key string          path to the public key file
  -s, --sig string          path to the signature file (default: <FILE>.minisig)
      --trusted-root string  Sigstore trusted-root path (sigstore-verify module only)
```

### video - Download videos from YouTube and other platforms
```bash
omni video [URL] [flags]
      --cookies string      Netscape cookie file path for interactive mode
      --interactive         start interactive mode (same as 'video interactive')
      --proxy string        HTTP/SOCKS proxy URL for interactive mode
  -v, --verbose             verbose output for interactive mode
```

### which - Locate a command
```bash
omni which [OPTION]... COMMAND... [flags]
  -a, --all                 print all matches
```

### xml - XML utilities (format, validate, convert)
```bash
omni xml [FILE] [flags]
  -i, --indent string       indentation string
  -m, --minify              minify XML (remove whitespace)
```

### xz - Compress or decompress xz files
```bash
omni xz [OPTION]... [FILE]... [flags]
  -d, --decompress          decompress
  -f, --force               force overwrite
  -k, --keep                keep original files
  -l, --list                list compressed file info
  -c, --stdout              write to stdout
  -v, --verbose             verbose mode
```

### xzcat - Decompress and print xz files
```bash
omni xzcat [FILE]...
```

### yaml - YAML utilities
```bash
omni yaml
```

### zcat - Decompress and print gzip files
```bash
omni zcat [FILE]...
```

## Command Tree

```
omni
+-- aicontext                                # Generate AI context for coding agents
+-- arch                                     # Print machine architecture
+-- attest                                   # Generate a signed SLSA provenance att...
|   \-- verify                               # Verify a SLSA provenance attestation ...
+-- awk                                      # Pattern scanning and processing language
+-- aws                                      # AWS CLI operations
|   +-- ec2                                  # AWS EC2 operations
|   |   +-- describe-instances               # Describe EC2 instances
|   |   +-- describe-security-groups         # Describe security groups
|   |   +-- describe-vpcs                    # Describe VPCs
|   |   +-- start-instances                  # Start EC2 instances
|   |   \-- stop-instances                   # Stop EC2 instances
|   +-- iam                                  # AWS IAM operations
|   |   +-- get-policy                       # Get IAM policy information
|   |   +-- get-role                         # Get IAM role information
|   |   +-- get-user                         # Get IAM user information
|   |   +-- list-policies                    # List IAM policies
|   |   \-- list-roles                       # List IAM roles
|   +-- s3                                   # AWS S3 operations
|   |   +-- cp                               # Copy files to/from S3
|   |   +-- ls                               # List S3 objects or buckets
|   |   +-- mb                               # Create an S3 bucket
|   |   +-- presign                          # Generate a presigned URL
|   |   +-- rb                               # Remove an S3 bucket
|   |   \-- rm                               # Remove S3 objects
|   +-- ssm                                  # AWS SSM Parameter Store operations
|   |   +-- delete-parameter                 # Delete a parameter
|   |   +-- get-parameter                    # Get a parameter value
|   |   +-- get-parameters                   # Get multiple parameters
|   |   +-- get-parameters-by-path           # Get parameters by path
|   |   \-- put-parameter                    # Create or update a parameter
|   \-- sts                                  # AWS STS operations
|       +-- assume-role                      # Assume an IAM role
|       \-- get-caller-identity              # Get details about the IAM identity ca...
+-- banner                                   # Generate ASCII art text banners
+-- base32                                   # Base32 encode or decode data
+-- base58                                   # Base58 encode or decode data (Bitcoin...
+-- base64                                   # Base64 encode or decode data
+-- basename                                 # Strip directory and suffix from file ...
+-- bbolt                                    # BoltDB database management
|   +-- buckets                              # List all buckets in the database
|   +-- check                                # Verify database integrity
|   +-- compact                              # Compact database to a new file
|   +-- create-bucket                        # Create a new bucket
|   +-- delete                               # Delete a key from a bucket
|   +-- delete-bucket                        # Delete a bucket
|   +-- dump                                 # Dump all keys and values in a bucket
|   +-- get                                  # Get value for a key
|   +-- info                                 # Display database information
|   +-- keys                                 # List keys in a bucket
|   +-- page                                 # Hex dump of a specific page
|   +-- pages                                # List database pages
|   +-- put                                  # Store a key-value pair
|   \-- stats                                # Display database statistics
+-- brdoc                                    # Brazilian document utilities (CPF, CNPJ)
|   +-- cnpj                                 # CNPJ operations (generate, validate, ...
|   \-- cpf                                  # CPF operations (generate, validate, f...
+-- buf                                      # Protocol buffer utilities (lint, form...
|   +-- breaking                             # Check for breaking changes
|   +-- compile                              # Compile proto files
|   +-- format                               # Format proto files
|   +-- generate                             # Generate code from proto files
|   +-- lint                                 # Lint proto files
|   +-- ls-files                             # List proto files in the module
|   \-- mod                                  # Module management commands
|       +-- init                             # Initialize a new buf module
|       \-- update                           # Update dependencies
+-- bunzip2                                  # Decompress bzip2 files
+-- bzcat                                    # Decompress and print bzip2 files
+-- bzip2                                    # Decompress bzip2 files
+-- case                                     # Text case conversion utilities
|   +-- all                                  # Show all case conversions
|   +-- camel                                # Convert to camelCase
|   +-- constant                             # Convert to CONSTANT_CASE
|   +-- detect                               # Detect the case type of text
|   +-- dot                                  # Convert to dot.case
|   +-- kebab                                # Convert to kebab-case
|   +-- lower                                # Convert to lowercase
|   +-- pascal                               # Convert to PascalCase
|   +-- path                                 # Convert to path/case
|   +-- sentence                             # Convert to Sentence case
|   +-- snake                                # Convert to snake_case
|   +-- swap                                 # Swap case of each character
|   +-- title                                # Convert to Title Case
|   +-- toggle                               # Toggle first character's case
|   \-- upper                                # Convert to UPPERCASE
+-- cat                                      # Concatenate files and print on the st...
+-- chmod                                    # Change file mode bits
+-- chown                                    # Change file owner and group
+-- cloud                                    # Cloud profile management
|   \-- profile                              # Manage cloud profiles
|       +-- add                              # Add a new cloud profile
|       +-- delete                           # Delete a cloud profile
|       +-- import                           # Import credentials from existing clou...
|       +-- list                             # List cloud profiles
|       +-- show                             # Show profile details
|       \-- use                              # Set a profile as default
+-- cmdtree                                  # Display command tree visualization
+-- cmp                                      # Compare two files byte by byte
+-- column                                   # Columnate lists
+-- comm                                     # Compare two sorted files line by line
+-- copy                                     # Alias for cp
+-- cp                                       # Copy files and directories
+-- crc32sum                                 # Compute and check CRC32 checksums
+-- crc64sum                                 # Compute and check CRC64 checksums
+-- cron                                     # Parse and explain cron expressions
+-- css                                      # CSS utilities (format, minify, validate)
|   +-- fmt                                  # Format/beautify CSS
|   +-- minify                               # Minify CSS
|   \-- validate                             # Validate CSS syntax
+-- csv                                      # CSV utilities (convert to/from JSON)
|   +-- fromjson                             # Convert JSON array to CSV
|   \-- tojson                               # Convert CSV to JSON array
+-- curl                                     # HTTP client with httpie-like syntax
+-- cut                                      # Remove sections from each line of files
+-- date                                     # Print the current date and time
+-- dd                                       # Convert and copy a file
+-- decrypt                                  # Decrypt data using AES-256-GCM
+-- df                                       # Report file system disk space usage
+-- diff                                     # Compare files line by line
+-- dirname                                  # Strip last component from file name
+-- dotenv                                   # Load environment variables from .env ...
+-- du                                       # Estimate file space usage
+-- echo                                     # Display a line of text
+-- egrep                                    # Print lines that match patterns (exte...
+-- encrypt                                  # Encrypt data using AES-256-GCM
+-- env                                      # Print environment variables
+-- exec                                     # Run external commands with credential...
+-- exist                                    # Check if files, directories, commands...
|   +-- command                              # Check if a command exists in PATH
|   +-- dir                                  # Check if a directory exists
|   +-- env                                  # Check if an environment variable is set
|   +-- file                                 # Check if a regular file exists
|   +-- path                                 # Check if any path exists (file, dir, ...
|   +-- port                                 # Check if a TCP port is listening
|   \-- process                              # Check if a process is running
+-- fgrep                                    # Print lines that match patterns (fixe...
+-- file                                     # Determine file type
+-- find                                     # Search for files in a directory hiera...
+-- fold                                     # Wrap each input line to fit in specif...
+-- for                                      # Loop and execute commands
|   +-- each                                 # Loop over a list of items
|   +-- glob                                 # Loop over files matching a pattern
|   +-- lines                                # Loop over lines from stdin or file
|   +-- range                                # Loop over a numeric range
|   \-- split                                # Loop over items split by delimiter
+-- free                                     # Display amount of free and used memor...
+-- gbc                                      # Git branch clean (alias)
+-- gh                                       # GitHub CLI shortcuts
|   +-- actions-rerun                        # Re-run a workflow run
|   +-- issue-mine                           # List issues assigned to you
|   +-- pr-approve                           # Approve a pull request
|   +-- pr-checkout                          # Check out a pull request
|   +-- pr-diff                              # Show diff for a pull request
|   \-- repo-clone-org                       # Clone all repositories in an organiza...
+-- git                                      # Git shortcuts and hacks
|   +-- amend                                # Amend last commit without editing
|   +-- blame-line                           # Blame specific line range
|   +-- branch-clean                         # Delete merged branches
|   +-- diff-words                           # Word-level diff
|   +-- fetch-all                            # Fetch all remotes with prune
|   +-- log-graph                            # Pretty log with graph
|   +-- pull-rebase                          # Pull with rebase
|   +-- push                                 # Push to remote
|   +-- quick-commit                         # Stage all and commit
|   +-- stash-staged                         # Stash only staged changes
|   +-- status                               # Short status
|   \-- undo                                 # Undo last commit (soft reset)
+-- gops                                     # List and signal running Go processes
|   +-- agent-cmd                            # Send an opcode to a target's embedded...
|   +-- inspect                              # Detail report for a single Go process...
|   +-- kill                                 # Signal one or more Go processes
|   +-- list                                 # List Go processes (default action of ...
|   +-- monitor                              # Sample CPU/memory/IO/FD metrics for a...
|   +-- obfuscation                          # Detect garble-style obfuscation in a ...
|   +-- profile                              # Capture a CPU profile from a target's...
|   +-- stream                               # Stream NDJSON runtime snapshots from ...
|   +-- top                                  # Interactive TUI dashboard of Go proce...
|   \-- trace                                # Capture a runtime trace from a target...
+-- gqc                                      # Git quick commit (alias)
+-- grep                                     # Print lines that match patterns
+-- gunzip                                   # Decompress gzip files
+-- gzip                                     # Compress or decompress files
+-- hash                                     # Compute and check file hashes
+-- head                                     # Output the first part of files
+-- hex                                      # Hexadecimal encoding and decoding uti...
|   +-- decode                               # Decode hexadecimal to text
|   \-- encode                               # Encode text to hexadecimal
+-- html                                     # HTML utilities (format, encode, decode)
|   +-- decode                               # HTML decode text
|   +-- encode                               # HTML encode text
|   +-- fmt                                  # Format/beautify HTML
|   +-- minify                               # Minify HTML
|   \-- validate                             # Validate HTML syntax
+-- id                                       # Print user and group information
+-- javaps                                   # List and signal running Java (JVM) pr...
|   +-- kill                                 # Signal one or more Java processes
|   \-- list                                 # List Java (JVM) processes
+-- join                                     # Join lines of two files on a common f...
+-- jq                                       # Command-line JSON processor
+-- json                                     # JSON utilities (format, minify, valid...
|   +-- fmt                                  # Beautify/format JSON with indentation
|   +-- fromcsv                              # Convert CSV to JSON array
|   +-- fromtoml                             # Convert TOML to JSON
|   +-- fromxml                              # Convert XML to JSON
|   +-- fromyaml                             # Convert YAML to JSON
|   +-- keys                                 # List all keys in JSON object
|   +-- minify                               # Compact JSON by removing whitespace
|   +-- stats                                # Show statistics about JSON data
|   +-- tocsv                                # Convert JSON array to CSV
|   +-- tostruct                             # Convert JSON to Go struct definition
|   +-- toxml                                # Convert JSON to XML
|   +-- toyaml                               # Convert JSON to YAML
|   \-- validate                             # Check if input is valid JSON
+-- jwt                                      # JWT (JSON Web Token) utilities
|   \-- decode                               # Decode and inspect a JWT token
+-- kconfig                                  # Show kubeconfig info
+-- kcs                                      # Switch kubectl context
+-- kdebug                                   # Debug pod with ephemeral container
+-- kdp                                      # Delete pods by selector
+-- kdrain                                   # Drain node for maintenance
+-- keb                                      # Exec into pod with bash
+-- kga                                      # Get all resources in namespace
+-- kge                                      # Get events sorted by time
+-- kill                                     # Send a signal to a process
+-- klf                                      # Follow pod logs with timestamps
+-- kns                                      # Switch default namespace
+-- kpf                                      # Quick port forward
+-- krr                                      # Rollout restart deployment
+-- krun                                     # Run a one-off pod
+-- kscale                                   # Scale deployment
+-- ksuid                                    # Generate K-Sortable Unique IDentifiers
+-- ktn                                      # Top nodes by resource usage
+-- ktp                                      # Top pods by resource usage
+-- kubectl                                  # Kubernetes CLI
+-- kwp                                      # Watch pods continuously
+-- less                                     # View file contents with scrolling
+-- lint                                     # Check Taskfiles for portability issues
+-- ln                                       # Make links between files
+-- loc                                      # Count lines of code by language
+-- logger                                   # Configure omni command logging
+-- ls                                       # List directory contents
+-- lsof                                     # List open files and network connections
+-- md5sum                                   # Compute and check MD5 message digest
+-- mkdir                                    # Create directories
+-- more                                     # View file contents page by page
+-- move                                     # Alias for mv
+-- mv                                       # Move (rename) files
+-- nanoid                                   # Generate compact, URL-safe unique IDs
+-- netstat                                  # Display network connections (alias fo...
+-- nl                                       # Number lines of files
+-- nodeps                                   # List and signal running Node.js proce...
|   +-- kill                                 # Signal one or more Node.js processes
|   \-- list                                 # List Node.js processes
+-- note                                     # Quick note taking to JSON in your Doc...
|   +-- add                                  # Add a new note entry
|   +-- list                                 # List note entries
|   \-- remove                               # Remove a note entry by index or ID
+-- paste                                    # Merge lines of files
+-- path                                     # Path manipulation utilities
|   +-- abs                                  # Return the absolute path
|   \-- clean                                # Return the shortest equivalent path w...
+-- pgrep                                    # Find processes by name or pattern
+-- pipe                                     # Chain omni commands without shell pipes
+-- pipeline                                 # Streaming text processing engine
+-- pkill                                    # Kill processes by name or pattern
+-- printf                                   # Format and print data
+-- project                                  # Analyze project structure, dependenci...
|   +-- deps                                 # Dependency analysis
|   +-- docs                                 # Documentation status check
|   +-- git                                  # Git repository info
|   +-- health                               # Health score (0-100) with grade
|   \-- info                                 # Full project overview
+-- ps                                       # Report a snapshot of current processes
+-- pwd                                      # Print working directory
+-- pyps                                     # List and signal running Python processes
|   +-- kill                                 # Signal one or more Python processes
|   \-- list                                 # List Python processes
+-- random                                   # Generate random values
+-- readlink                                 # Print resolved symbolic links or cano...
+-- realpath                                 # Print the resolved path
+-- remove                                   # Alias for rm
+-- repo                                     # Repository analysis tools
|   \-- analyze                              # Generate comprehensive repository con...
+-- reprocheck                               # Fail if any A/B build artifact pair d...
+-- rev                                      # Reverse lines characterwise
+-- rg                                       # Recursively search for a pattern (rip...
+-- rm                                       # Remove files or directories
+-- rmdir                                    # Remove empty directories
+-- sbom                                     # Generate an SBOM (SPDX 2.3 or Cyclone...
+-- scaffold                                 # Code scaffolding utilities
|   +-- cobra                                # Cobra CLI application generator
|   |   +-- add                              # Add a new command to an existing Cobr...
|   |   +-- add-tools                        # Add cmdtree and aicontext to an exist...
|   |   +-- config                           # Manage cobra generator configuration
|   |   \-- init                             # Initialize a new Cobra CLI application
|   +-- handler                              # Generate HTTP handler
|   +-- mcp                                  # Generate MCP server
|   +-- repository                           # Generate database repository
|   \-- test                                 # Generate tests for a Go source file
+-- scan                                     # Scan an SBOM against a signed OSV vul...
|   +-- db                                   # Manage the OSV vulnerability database
|   |   \-- update                           # Download and verify the OSV vulnerabi...
|   \-- source                               # Reachability-aware Go source scan (de...
+-- sed                                      # Stream editor for filtering and trans...
+-- seq                                      # Print a sequence of numbers
+-- sha256sum                                # Compute and check SHA256 message digest
+-- sha512sum                                # Compute and check SHA512 message digest
+-- shuf                                     # Generate random permutations
+-- sign                                     # Create a detached minisign signature
|   \-- keygen                               # Generate a passphrase-protected Ed255...
+-- sleep                                    # Delay for a specified amount of time
+-- snowflake                                # Generate Twitter Snowflake-style IDs
+-- sort                                     # Sort lines of text files
+-- split                                    # Split a file into pieces
+-- sql                                      # SQL utilities (format, minify, validate)
|   +-- fmt                                  # Format/beautify SQL
|   +-- minify                               # Minify SQL
|   \-- validate                             # Validate SQL syntax
+-- sqlite                                   # SQLite database management
|   +-- check                                # Verify database integrity
|   +-- columns                              # Show table columns
|   +-- dump                                 # Export database as SQL
|   +-- import                               # Import SQL file into database
|   +-- indexes                              # List all indexes
|   +-- query                                # Execute SQL query
|   +-- schema                               # Show table schema
|   +-- stats                                # Display database statistics
|   +-- tables                               # List all tables in the database
|   \-- vacuum                               # Optimize database
+-- ss                                       # Display socket statistics
+-- stat                                     # Display file or file system status
+-- strings                                  # Print the printable strings in files
+-- tac                                      # Concatenate and print files in reverse
+-- tagfixer                                 # Fix and standardize Go struct tags
|   \-- analyze                              # Analyze struct tag usage patterns
+-- tail                                     # Output the last part of files
+-- tar                                      # Create, extract, or list archive files
+-- task                                     # Run tasks defined in Taskfile.yml
+-- terraform                                # Terraform CLI
|   +-- apply                                # Apply changes to infrastructure
|   +-- console                              # Interactive console
|   +-- destroy                              # Destroy managed infrastructure
|   +-- fmt                                  # Format configuration files
|   +-- get                                  # Download modules
|   +-- graph                                # Generate dependency graph
|   +-- import                               # Import existing infrastructure
|   +-- init                                 # Initialize Terraform working directory
|   +-- output                               # Show output values
|   +-- plan                                 # Create execution plan
|   +-- providers                            # Show provider information
|   +-- refresh                              # Refresh state
|   +-- show                                 # Show plan or state
|   +-- state                                # State management commands
|   |   +-- list                             # List resources in state
|   |   +-- mv                               # Move resource in state
|   |   +-- rm                               # Remove resources from state
|   |   \-- show                             # Show a resource in state
|   +-- taint                                # Mark resource for recreation
|   +-- test                                 # Run tests
|   +-- untaint                              # Remove taint from resource
|   +-- validate                             # Validate configuration
|   +-- version                              # Show Terraform version
|   \-- workspace                            # Workspace management commands
|       +-- delete                           # Delete workspace
|       +-- list                             # List workspaces
|       +-- new                              # Create new workspace
|       +-- select                           # Select workspace
|       \-- show                             # Show current workspace
+-- testcheck                                # Check test coverage for Go packages
+-- time                                     # Time a simple command or give resourc...
+-- toml                                     # TOML utilities
|   +-- fmt                                  # Format TOML
|   \-- validate                             # Validate TOML syntax
+-- top                                      # Display system processes sorted by re...
+-- touch                                    # Update the access and modification ti...
+-- tr                                       # Translate or delete characters
+-- tree                                     # Display directory tree structure
+-- ulid                                     # Generate Universally Unique Lexicogra...
+-- uname                                    # Print system information
+-- uniq                                     # Report or omit repeated lines
+-- unxz                                     # Decompress xz files
+-- unzip                                    # Extract files from a zip archive
+-- uptime                                   # Tell how long the system has been run...
+-- url                                      # URL encoding and decoding utilities
|   +-- decode                               # URL decode text
|   \-- encode                               # URL encode text
+-- uuid                                     # Generate random UUIDs
+-- vault                                    # HashiCorp Vault CLI
|   +-- delete                               # Delete secrets
|   +-- kv                                   # KV secrets engine operations
|   |   +-- delete                           # Delete a secret from KV
|   |   +-- destroy                          # Permanently destroy secret versions
|   |   +-- get                              # Get a secret from KV
|   |   +-- list                             # List secrets in KV
|   |   +-- metadata                         # KV metadata operations
|   |   |   +-- delete                       # Delete all versions and metadata
|   |   |   \-- get                          # Get secret metadata
|   |   +-- put                              # Put a secret into KV
|   |   \-- undelete                         # Restore deleted secret versions
|   +-- list                                 # List secrets
|   +-- login                                # Authenticate to Vault
|   +-- read                                 # Read secrets
|   +-- status                               # Show Vault server status
|   +-- token                                # Token operations
|   |   +-- lookup                           # Lookup current token
|   |   +-- renew                            # Renew current token
|   |   \-- revoke                           # Revoke current token
|   \-- write                                # Write secrets
+-- verify                                   # Verify a detached minisign signature ...
+-- video                                    # Download videos from YouTube and othe...
|   +-- auth                                 # Extract YouTube cookies from Chrome b...
|   +-- channel                              # Download all videos from a YouTube ch...
|   +-- download                             # Download video(s) from URL
|   +-- extractors                           # List all supported sites/extractors
|   +-- info                                 # Show video metadata as JSON
|   +-- interactive                          # Interactive video menu (formats, info...
|   +-- list-formats                         # List available download formats
|   \-- search                               # Search YouTube for videos
+-- watch                                    # Execute a program periodically, showi...
+-- wc                                       # Print newline, word, and byte counts ...
+-- which                                    # Locate a command
+-- whoami                                   # Print effective username
+-- xargs                                    # Build and execute command lines from ...
+-- xml                                      # XML utilities (format, validate, conv...
|   +-- fmt                                  # Format XML
|   +-- fromjson                             # Convert JSON to XML
|   +-- tojson                               # Convert XML to JSON
|   \-- validate                             # Validate XML syntax
+-- xxd                                      # Make a hex dump or reverse it
+-- xz                                       # Compress or decompress xz files
+-- xzcat                                    # Decompress and print xz files
+-- yaml                                     # YAML utilities
|   +-- fmt                                  # Format YAML
|   +-- k8s                                  # Format YAML with Kubernetes conventions
|   +-- tostruct                             # Convert YAML to Go struct definition
|   \-- validate                             # Validate YAML syntax
+-- yes                                      # Output a string repeatedly until killed
+-- yq                                       # Command-line YAML processor
+-- zcat                                     # Decompress and print gzip files
\-- zip                                      # Package and compress files into a zip...
```
